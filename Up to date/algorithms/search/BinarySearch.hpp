#ifndef BINARY_SEARCH_HPP
#define BINARY_SEARCH_HPP

#include <vector>

class BinarySearch
{
public:
    int static search(const int numberToSearch, const std::vector<int> &vectorToSearch);
};

#endif